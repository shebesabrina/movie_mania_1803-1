
  <%= @genres.each do |genre|%>
  <%= link_to genre.name, genre_path(genre) %>
  <% end %>

<% if current_admin? %>
  <%= form_for [:admin, @genre] do |f| %>
    Genre name: <%= f.text_field :name %><br />
    <%= f.submit %>
  <% end %>
<% end %>
